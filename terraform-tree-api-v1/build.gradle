archivesBaseName = "terraform-tree-api-v1"
version = formatVersion(project, "4.0.0-alpha.2")

dependencies {
	modImplementation fabricApi.module("fabric-object-builder-api-v1", project.fabric_version)
}